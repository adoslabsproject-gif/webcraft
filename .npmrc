# Strict version resolution: prevent silent dependency drift
save-exact=true
engine-strict=true
fund=false
audit=false

# pnpm-specific: hoist only what's necessary for tooling
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Lockfile: always commit, never auto-fix
prefer-frozen-lockfile=true
