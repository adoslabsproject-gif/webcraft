/// WebCraft AI tools — tool definitions consumed by ai-router and surfaced as model tool_use schemas.
///
/// Phase 0 scaffold: empty. Will export:
///   - bash, edit, write, read, grep, glob, find, sandbox_run, db_query, http_fetch

export {};
