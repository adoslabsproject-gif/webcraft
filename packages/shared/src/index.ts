/// WebCraft shared types & schemas — IPC contracts between renderer and Node sidecar.
///
/// Phase 0 scaffold: empty. Will host zod schemas (FileOp, ToolCall, ChatMessage,
/// DbConnection, DevServerSpec, ...) consumed by both renderer and sidecar via tRPC.

export const WEBCRAFT_VERSION = '0.1.0';
